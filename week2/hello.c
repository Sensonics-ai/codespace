#include <stdio.h>

int main(void)
{
    printf("hello, world\n");
}

//clang is basically (C languaje). Take note on how its basically doing what make was doing for us. C lang is able to take "command line arguments".
//It's just an additional word that modifies or specifies a bit that command