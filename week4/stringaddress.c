#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string s = "Hi!";   //s is a string, and it points to the address of the first character of the string
    printf("%s\n", s);  //prints the string

}