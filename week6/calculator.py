from cs50 import get_int            # Importing the get_int function from the cs50 library

x = get_int("x: ")      # Using the get_int function to get the user's input and storing it in the variable x
y = get_int("y: ")      # Using the get_int function to get the user's input and storing it in the variable y

print(x + y)            # Printing the sum of x and y